import Link from 'next/link';

export default function SampleQuestion() {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Sample Computer Science Question</h1>
      <p>
        Here is a simple programming problem to get you started:
      </p>
      <h3>Problem: Reverse a String</h3>
      <p>
        Write a function that takes a string as input and returns the string in reversed order. For example, given the string "hello", the output should be "olleh".
      </p>
    </div>
  );
}
